import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  image: {
    height: '25%',
    width: '100%',
    borderRadius: 7,
  },
  title: {
    fontSize: 22,
    fontWeight: '900', // 100 - 900
    color: 'black',
    textAlign: 'center',
    lineHeight: 35,
    marginBottom: 5,
  },
});
